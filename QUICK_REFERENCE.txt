╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║         INTEGRATED NETWORK ANOMALY DETECTION - QUICK REFERENCE               ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ QUICK START                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Find your network interface:
     $ ip -br link show

  2. Start the application:
     $ sudo ./start_integrated_app.sh enp1s0

  3. Open web browser:
     → http://localhost:8000

┌──────────────────────────────────────────────────────────────────────────────┐
│ COMMANDS                                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  # Using startup script (easiest)
  sudo ./start_integrated_app.sh <interface> [threshold]
  
  Examples:
  sudo ./start_integrated_app.sh enp1s0
  sudo ./start_integrated_app.sh eth0 0.7
  sudo ./start_integrated_app.sh wlan0 0.8

  # Direct Python command
  sudo python3 integrated_web_app.py --interface enp1s0 --threshold 0.7

  # With custom port
  sudo python3 integrated_web_app.py -i enp1s0 -t 0.7 -p 9000

┌──────────────────────────────────────────────────────────────────────────────┐
│ ARGUMENTS                                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  --interface, -i    Network interface (REQUIRED)
                     Examples: eth0, enp1s0, wlan0
  
  --threshold, -t    Detection sensitivity (default: 0.7)
                     Range: 0.0 - 1.0
                     Lower = More sensitive, more detections
                     Higher = Less sensitive, fewer false positives
  
  --port, -p         Web server port (default: 8000)

┌──────────────────────────────────────────────────────────────────────────────┐
│ THRESHOLD GUIDE                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  0.5 - 0.6   Very Sensitive    ⚠️  Many detections, high false positives
  0.6 - 0.7   Balanced          ✓  Good for testing and learning
  0.7 - 0.8   Recommended       ✓  Production use, fewer false positives
  0.8 - 0.9   Conservative      ✓  Only high-confidence threats
  0.9 - 1.0   Very Strict       ⚠️  May miss some real threats

┌──────────────────────────────────────────────────────────────────────────────┐
│ COMMON INTERFACES                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  eth0, eth1         Traditional Ethernet naming
  enp1s0, enp0s3     Modern Ethernet naming (systemd)
  wlan0, wlp2s0      WiFi interfaces
  lo                 Loopback (don't use for monitoring)

  To find yours: ip -br link show | grep -v lo

┌──────────────────────────────────────────────────────────────────────────────┐
│ WEB INTERFACE                                                                │
└──────────────────────────────────────────────────────────────────────────────┘

  Main Interface:    http://localhost:8000
  Statistics API:    http://localhost:8000/api/stats
  Anomalies API:     http://localhost:8000/api/anomalies?limit=50

  From another PC:   http://<server-ip>:8000

┌──────────────────────────────────────────────────────────────────────────────┐
│ SEVERITY LEVELS                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  🔴 HIGH      Confidence ≥ 90%    Critical threat, investigate immediately
  🟡 MEDIUM    Confidence ≥ 75%    Suspicious activity, review
  🟢 LOW       Confidence < 75%    Possible anomaly, monitor

┌──────────────────────────────────────────────────────────────────────────────┐
│ STOPPING THE APPLICATION                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Press Ctrl+C in the terminal
  
  Logs will be saved to: logs/
  - anomalies_YYYYMMDD_HHMMSS.csv
  - flows_YYYYMMDD_HHMMSS.csv
  - session_YYYYMMDD_HHMMSS.json

┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  ❌ "requires root/sudo privileges"
     → Run with sudo: sudo python3 integrated_web_app.py -i enp1s0

  ❌ "Interface not found"
     → Check available: ip -br link show
     → Use correct name: sudo python3 integrated_web_app.py -i enp1s0

  ❌ "Connection refused" / Page won't load
     → Check if running: curl http://localhost:8000/api/stats
     → Try different port: sudo python3 integrated_web_app.py -i enp1s0 -p 8080

  ❌ No anomalies showing
     → Lower threshold: --threshold 0.6
     → Generate traffic: browse websites, download files
     → Wait 2-3 minutes for flow patterns

  ❌ Page shows "Waiting for anomalies..."
     → This is normal! Just means no threats detected yet
     → Generate some network activity
     → Try lower threshold

┌──────────────────────────────────────────────────────────────────────────────┐
│ TESTING THE SETUP                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Start the application:
     sudo ./start_integrated_app.sh enp1s0 0.6

  2. Open browser:
     http://localhost:8000

  3. Generate network traffic:
     - Browse multiple websites
     - Download a file
     - Run: ping -c 100 google.com

  4. Watch for anomalies to appear in real-time

┌──────────────────────────────────────────────────────────────────────────────┐
│ FILES CREATED                                                                │
└──────────────────────────────────────────────────────────────────────────────┘

  integrated_web_app.py           Main application
  start_integrated_app.sh         Quick start script
  INTEGRATED_APP_GUIDE.md         Full documentation
  
  logs/
    ├── anomalies_*.csv           Detected anomalies (CSV)
    ├── anomalies_*.json          Detected anomalies (JSON)
    ├── flows_*.csv               All network flows
    └── session_*.json            Session summary

┌──────────────────────────────────────────────────────────────────────────────┐
│ EXAMPLE USAGE SESSION                                                        │
└──────────────────────────────────────────────────────────────────────────────┘

  # Start monitoring WiFi with medium sensitivity
  $ sudo ./start_integrated_app.sh wlan0 0.7
  
  ================================================================
  INTEGRATED NETWORK ANOMALY DETECTION SYSTEM
  ================================================================
  Interface:       wlan0
  Threshold:       0.7
  Web Interface:   http://localhost:8000
  ================================================================
  
  [Open http://localhost:8000 in browser]
  
  → Dashboard shows real-time statistics
  → Anomalies appear as they're detected
  → Click on anomalies for details
  
  [Press Ctrl+C when done]
  
  Shutting down...
  Stopped.

┌──────────────────────────────────────────────────────────────────────────────┐
│ TIPS                                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  ✓ Start with threshold 0.7 for balanced detection
  ✓ Run for at least 5-10 minutes to see patterns
  ✓ Generate various types of network activity for testing
  ✓ Keep browser tab open for real-time updates
  ✓ Check logs/ directory for historical data
  ✓ Use API endpoints for custom integrations

┌──────────────────────────────────────────────────────────────────────────────┐
│ NEED HELP?                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  Full Guide:        cat INTEGRATED_APP_GUIDE.md
  Check Interface:   ip -br link show
  Test Capture:      sudo tcpdump -i enp1s0 -c 10
  View Logs:         ls -lah logs/
  API Test:          curl http://localhost:8000/api/stats

╔══════════════════════════════════════════════════════════════════════════════╗
║  Ready to start? Run: sudo ./start_integrated_app.sh <your-interface>       ║
╚══════════════════════════════════════════════════════════════════════════════╝
